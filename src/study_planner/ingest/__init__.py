"""Document ingestion: OCR + (later) deterministic handbook parsing + retrieval.

Kept separate from tools/pdf_tools.py (the small CV/transcript reads) because the
handbook path needs heavier machinery for large/scanned catalogs.
"""
